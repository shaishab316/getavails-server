model Chat {
  id         String   @id @default(uuid())
  created_at DateTime @default(now())

  user_ids        String[]
  last_message_id String?

  messages Message[]

  @@unique([user_ids])
  @@map("chats")
}
