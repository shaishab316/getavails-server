model Chat {
  id         String   @id @default(uuid())
  created_at DateTime @default(now())

  user_ids String[]

  messages Message[]
  users    User[]    @relation("UserChats")

  @@unique([user_ids])
  @@map("chats")
}
